import React from "react";
import { Tile } from "react-native-elements";

const About = () => {
  return (
    <Tile
      title="Make a list"
      caption={`Do you forget to do things when you go outside. /n
                No worries...`}
    />
  );
};

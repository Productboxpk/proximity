import React from "react";
import { Tile } from "react-native-elements";

const About = () => {
  return (
    <Tile
      title="Set proximity"
      caption={`Already done with the list. 
                Now let the app know when to remind you of the 
                list, by setting a range you let the app reminds you when 
                you go out of range`}
    />
  );
};
